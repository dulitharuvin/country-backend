server:
  port: 9090

spring:
  application:
    name: country-backend
  datasource:
    url: jdbc:mysql://localhost:3306/country_info?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: H@si0116
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: none
